import styled from 'styled-components';

export const Container = styled.div`
  background: linear-gradient(180deg, #ff0000, #f91e96);
  padding: 40px 0;
  display: flex;
  justify-content: center;
  svg {
    margin: 0 24px;
  }
`;
